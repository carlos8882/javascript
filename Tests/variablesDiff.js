var varvar = 'varvar test'; // it is like global variable, it was the older form to declare variables
let letvar = 'letvar test'; // it is a local variable, this variable is alive until the function will finish
const constvar = 'constvar test'; // As let, this variable will still alive until the function will finish, gererally it is used to declare constants

console.log (varvar);
console.log (letvar);
console.log (constvar);

// console.log('Hola mundo');
